{
  "name": "ui-locator-repair-playground",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve app -l 3000",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "serve": "^14.2.0"
  }
}
